name: Deploy to Convex

on:
  push:
    branches:
      - demo

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          
      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8.15.6

      - name: Install dependencies
        run: pnpm install

      # [THE FIX] Pass CONVEX_DEPLOY_KEY explicitly as a token
      - name: Deploy Convex backend
        run: npx convex deploy --token ${{ secrets.CONVEX_DEPLOY_KEY }}
        env:
          # Remove CONVEX_DEPLOY_KEY from the env: block since it's passed directly above
          # CONVEX_DEPLOY_KEY: ${{ secrets.CONVEX_DEPLOY_KEY }}
          # Keep this empty or remove the env block entirely if not needed
          # We will remove the env block for simplicity
